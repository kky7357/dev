
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://103.218.157.98:3306/mysql?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: kykim
  thymeleaf:
    prefix : classpath:templates/
    suffix : .html
    cache : false
  devtools:
    livereload:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: false
    show-sql: true
    hibernate:
      format_sql: true
      ddl-auto: create

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE
server:
  context-path=/users:
